# Example vault

Open **this folder** as an Obsidian vault (the folder that contains `wiki/` and `raw/`).

Two contributors ingested without editing the same files:

- Alice: `raw/alice/karpathy-llm-wiki/` → source + contribution + log + alias
- Bob: `raw/bob/autosci-discover/` → source + contribution + log

Then a serial compile wrote `wiki/hubs/llm-wiki.md`. A human pin lives in `wiki/pins/llm-wiki/`.

Catalogs are the `.base` files, not `index.md`.
